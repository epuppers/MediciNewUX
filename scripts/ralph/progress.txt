# Progress Log — Polish & Corrections

## Codebase Patterns
- ALL colors must use `var(--token)` from tokens.css — no raw hex/rgb values
- For alpha colors use `rgba(var(--violet-3-rgb), 0.1)` pattern
- Dark mode overrides go in the SAME CSS file using `[data-theme="dark"]` selector
- Animation gating: wrap animations with `[data-a11y-motion="reduced"]`
- Event delegation: all handlers go in `initEventListeners()` — no inline handlers
- Beveled borders (raised): `border-color: var(--taupe-2) var(--taupe-3) var(--taupe-3) var(--taupe-2)`
- Beveled borders (inset): `border-color: var(--taupe-3) var(--taupe-1) var(--taupe-1) var(--taupe-3)`
- Font stack: ChicagoFLF (pixel headings), IBM Plex Mono (UI/code), DM Sans (body)
- Border radii: `var(--r-sm)` (3px), `var(--r-md)` (4px), `var(--r-lg)` (6px) — never raw px
- Dev server: `python3 -m http.server 8082` serves at http://localhost:8082/index.html
- Commit format: `polish [task ID]: [brief description]`
- localStorage keys used: theme, purpleIntensity, a11yFontSize, a11yDyslexia, a11yMotion, a11yContrast
- The Erabor thread input area (lines 976-1008 of index.html) is the non-standard one; Fund III (lines 417-456) is the canonical pattern
- .typing-dot in components.css line 879 — currently uses var(--r-md) not 50%
- UI.buildMiniCalendar() in app.js line 847 — hardcoded day array, needs computed weekday offset
- Memory cards: .mem-fact-card in components.css line 1182 — flat borders need bevel
- Lesson cards: .lesson-card in components.css — flat borders need bevel
- Profile menu: #profilePanel in layout.css line 388 — currently flat list of a11y toggles
- Brain nav: .brain-nav in components.css line 2322 — margin-top:auto pushes to bottom, no collapse mechanism
- Sidebar: .sidebar.collapsed CSS exists at layout.css line 77 — but no toggle button or JS
- File attachments in AI responses: .file-attachment in chat.css line 648 — bevel card with icon, name, meta, action link
- Workflow file chips in user messages: .wf-file-chip in chat.css line 1082 — small inline chips
- Input area structure: .input-area > .input-row > [.text-input, .attach-wrapper, .cmd-btn]. New file strip goes between .input-area top and .input-row
- Drag-drop zone: .input-area.drop-active in chat.css line 311 — already shows 'Drop files here' overlay
- Fund III thread last message is Message 4 (AI with wash sale flags) — new user Message 5 with files goes after it

---

## Iteration 1 — Task P-01: Fix sidebar logo and title vertical alignment
- What was done: Changed `.logo-row` `align-items` from `flex-start` to `center` in layout.css line 115. This vertically centers the 6-sphere logo mark with the COSIMO title and MEDICI & COMPANY subtitle text block.
- Files modified: css/layout.css
- Gotchas / learnings: Simple one-line change. No dark mode impact since align-items is theme-independent.

## Iteration 2 — Task P-02: Add sidebar collapse/expand toggle button
- What was done: Added chevronLeft/chevronRight icons to icons.js. Added .sidebar-toggle-btn to the .logo-row in index.html. Created UI.toggleSidebar() in app.js with localStorage persistence ('sidebarCollapsed') and on-load restore. Added event listener in initEventListeners(). Added CSS in layout.css for the toggle button (hover/active/focus-visible states, dark mode overrides). Updated collapsed rules: sidebar-header stays visible (with reduced padding) so toggle button remains accessible; sidebar-search and brain-nav are hidden when collapsed; resize handle is hidden when collapsed.
- Files modified: js/icons.js, index.html, css/layout.css, js/app.js
- Gotchas / learnings: The existing .sidebar.collapsed rules hid .sidebar-header entirely, but we need it visible for the toggle button. Changed to hide individual children (.sidebar-search, .new-btn) instead. The resize handle is a sibling of #sidebar, not a child, so the CSS selector uses `.sidebar.collapsed + .resize-handle-sidebar` (adjacent sibling). Also added `.sidebar.collapsed .resize-handle-sidebar` for safety plus JS style.display toggling.

## Iteration 3 — Task P-03: Make Brain nav section collapsible
- What was done: Added a chevron toggle button to the Brain nav header that collapses/expands the 3 nav buttons (Memory, Lessons, Graphs). Uses `.brain-nav-collapsed` class on `#brainNav` to hide `.brain-nav-items` via max-height transition. Chevron rotates -90deg when collapsed. State persisted in localStorage (`brainNavCollapsed`). Restored on page load. Brain view remains visible when nav is collapsed — only sidebar space is affected.
- Files modified: index.html (added `.brain-nav-header` wrapper with toggle button), css/components.css (new styles for `.brain-nav-header`, `.brain-nav-toggle`, collapse animation, dark mode overrides), js/app.js (added `UI.toggleBrainNav()`, localStorage restore IIFE, event listener in `initEventListeners()`)
- Gotchas / learnings: The `.side-label` inside `.brain-nav` needed to be re-targeted as `.brain-nav-header .side-label` since the label is now inside the new header wrapper. The max-height approach (200px → 0) works well for the 3-button list. The chevron uses CSS transform rotate(-90deg) when collapsed rather than swapping icons, which is simpler than the sidebar toggle's icon-swap approach.

## Iteration 4 — Task P-04: Restructure profile menu with sub-menus
- What was done: Restructured #profilePanel into a two-view system: main menu (user info, Appearance & Accessibility item with chevron, Account Settings with toast, Sign Out) and a sub-panel (back arrow header + all existing toggles/sliders). Uses `.show-subpanel` class on `#profilePanel` to toggle between views via CSS display switching. Slide animations (translateX) gated by `[data-a11y-motion="reduced"]`. Panel widens to 240px when sub-panel is shown. Added `arrowLeft` icon to icons.js. Added `UI.showProfileSubpanel()`, `UI.showProfileMain()` functions. Profile menu resets to main view each time it's opened. All existing toggle functionality preserved in the sub-panel.
- Files modified: js/icons.js, index.html, css/layout.css, js/app.js
- Gotchas / learnings: The profile panel event handler uses `data-action` attributes for the new menu items (appearance, account-settings, profile-back), checked before the existing theme/toggle handlers. The `.show-subpanel` class approach (show/hide via display) is simpler than animated panel sliding since we just need a view swap. The panel width change (200px → 240px) only happens when sub-panel is active via `.th-dropdown-profile.show-subpanel { width: 240px }`.

## Iteration 5 — Task P-05: Elevate Memory page card depth to match app bevels
- What was done: Updated .mem-role-card, .mem-personality, and .mem-fact-card to use raised bevel pattern (border-color: taupe-2 taupe-3 taupe-3 taupe-2). Added 3px left-border accent per category color on .mem-fact-card using data-category attribute selectors (preference=berry-3, workflow=green, contact=blue-3, fund=violet-3, style=amber, context=taupe-3). Added inset background tint to .mem-facts-toolbar (rgba black 0.02, with padding/radius) and changed .mem-search-input to inset bevel (border-color: taupe-3 taupe-1 taupe-1 taupe-3) with off-white background. Changed .mem-cat-pill.active from taupe-5 to violet-3 for higher contrast. Full dark mode overrides for all changes.
- Files modified: css/components.css
- Gotchas / learnings: The fact cards use data-category attribute (set in app.js line 3287), which maps directly to category names (preference, workflow, contact, fund, style, context). Category color tokens already exist as .cat-* classes in components.css — the left-border accents reuse the same color logic. The hover state on .mem-fact-card was bumped up one shade (taupe-3/taupe-4) since the default border is now taupe-2/taupe-3.

## Iteration 6 — Task P-06: Upgrade Memory from plain text to knowledge graph entity chips
- What was done: Added `linkedEntities` array to each fact in MOCK_MEMORY (2-3 entities per relevant fact, empty array for facts without entities). Updated `renderMemoryFromData()` to render `.mem-entity-chips` container with `.mem-entity-chip` buttons below fact text, using inline icons mapped by type (person→person, fund→building, property→folder, document→lessons). Added `Graph.selectByName()` function that searches MOCK_GRAPH_DATA.nodes by label and navigates to the entity (or shows toast if not found). Added click handler in `initEventListeners()` memFactList delegation that switches to graphs brain section and calls `Graph.selectByName()`. Added CSS for `.mem-entity-chip` with pill shape, bevel border, hover/active/focus-visible states, and full dark mode overrides.
- Files modified: js/mock-data.js, js/app.js, css/components.css
- Gotchas / learnings: Entity type→icon mapping: person→ICONS.person, fund→ICONS.building, property→ICONS.folder, document→ICONS.lessons. The `icon()` helper from icons.js is used to render with custom size (10×10). Graph.selectByName uses a 150ms setTimeout before calling openGraphEntity to allow the graph SVG to build first if not already built. Some entities like "Yardi Voyager" and "Deloitte" won't be found in the graph data — the toast fallback handles this gracefully.

## Iteration 7 — Task P-07: Elevate Lessons page card depth to match app bevels
- What was done: Updated .lesson-card to use raised bevel pattern (border-color: taupe-2 taupe-3 taupe-3 taupe-2) with 3px violet left-border accent. Changed .scope-company badge to solid blue-3 bg with white text, .scope-user to solid violet-3 bg with white text for better contrast. Added bevel styling to .lesson-block (non-edit state) with raised border pattern and white background. Bumped hover state to taupe-3/taupe-4 + violet-4 left border. Full dark mode overrides for all changes.
- Files modified: css/components.css
- Gotchas / learnings: The .lesson-block only had styles under the [contenteditable="true"] selector (edit mode — dashed violet border). Added a base .lesson-block rule for the normal view state with raised bevel. The scope badges were previously transparent bg + colored text which was low contrast — solid bg + white text is much more readable. Dark mode card border uses taupe-3/taupe-4 (one shade darker than light mode) to maintain the bevel depth against the darker background.

## Iteration 8 — Task P-08: Fix Erabor thread input area to match standard pattern
- What was done: Replaced the Erabor input area HTML with the standard Fund III pattern: (1) swapped text "Attach" button for icon-based .attach-wrapper with .attach-menu (computer + cloud icons), (2) removed the explicit .cmd-stop button, (3) reordered .input-footer so model-selector comes first, then keyboard hint text. Removed all erabor-specific IDs (erabor-input-area, erabor-input, erabor-send-btn, erabor-stop-btn). Updated JS: Chat.showEraborStopBtn() now queries .cmd-primary within #thread-erabor instead of by ID; Chat.cancelErabor() finds the text input via thread query; removed the stop button event listener since there's no dedicated stop button in the standard pattern.
- Files modified: index.html, js/app.js
- Gotchas / learnings: Chat.disableInput('erabor', ...) already uses class-based query (finds .text-input within #thread-erabor), so it worked without changes. The showEraborStopBtn function still hides/shows the send button during the streaming animation — it just no longer swaps to a stop button. The cancelErabor function's stop functionality is lost (no button to click), but since this is a demo animation it's acceptable.

## Iteration 9 — Task P-09: Change typing indicator dots from squares to spheres matching logo
- What was done: Updated .typing-dot in components.css to use border-radius: 50% (was var(--r-md)), radial-gradient background matching .logo-sphere.dark (radial-gradient at 35% 30%, violet-3 to violet-4), and inset box-shadow matching logo spheres. Added dark mode override with slightly stronger shadow values. The typing-bounce animation is already caught by the global reduced motion rule at layout.css:782.
- Files modified: css/components.css
- Gotchas / learnings: The logo sphere styling is at layout.css:214-216 (.logo-sphere.dark). The typing dots exactly mirror those values. Dark mode doesn't need different gradient colors (tokens auto-remap), just slightly boosted shadow alphas (0.25→0.4 for dark shadow, 0.08→0.12 for light highlight) to maintain visual depth against dark backgrounds.

## Iteration 10 — Task P-10: Fix calendar dropdown grid layout
- What was done: Replaced hardcoded day array in UI.buildMiniCalendar() with computed logic. Parses month/year from MOCK_CALENDAR.month, computes first weekday via Date constructor, derives event days from MOCK_CALENDAR.events meta fields using regex, and marks day 13 as today. March 1, 2026 is a Sunday (getDay()=0), so 0 leading empties instead of the previous incorrect 2.
- Files modified: js/app.js
- Gotchas / learnings: MOCK_CALENDAR.events meta format is "Mar 12, 10:00 AM" — regex /\b(\d{1,2}),/ extracts the day number. The today value is hardcoded to '13' matching the project's currentDate (March 13, 2026) rather than using new Date() since this is a static demo with mock data.

## Iteration 11 — Task P-11: File upload preview strip and user message file attachments
- What was done: (A) Added .input-file-strip with 2 mock staged file chips (Q1_Holdings_Report.pdf, Fund_III_NAV_032026.xlsx) above the .input-row in the Fund III input area. Each chip has file-type icon, filename (truncated), size, and × remove button. Chips are horizontally scrollable on overflow. (B) Added Message 5 to Fund III thread — user message from Eliot at 2:38 PM with .msg-file-attachments containing 2 .file-attachment cards (PDF with blue-3 icon via .file-icon-pdf class, xlsx with default green icon). Added remove-chip event handler in initEventListeners() that removes the chip and cleans up the strip if empty.
- Files modified: index.html, css/chat.css, js/app.js
- Gotchas / learnings: tokens.css only has blue-1/2/3 (no blue-4 or blue-hi/lo), so .file-icon-pdf bevel uses blue-2 and blue-3 for the border. The .msg-file-attachments container resets margin on child .file-attachment elements (margin: 0) since the default .file-attachment has margin-left: 30px which doesn't apply inside user cards. Used a CSS class (.file-icon-pdf) instead of inline style for the PDF icon color to comply with no-inline-styles rule.

