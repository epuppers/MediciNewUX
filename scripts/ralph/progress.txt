# Progress Log — Workflows Rebuild

## Codebase Patterns
- ALL colors must use `var(--token)` from tokens.css — no raw hex/rgb values anywhere
- For alpha colors use `rgba(var(--violet-3-rgb), 0.1)` pattern (RGB triplet tokens defined in tokens.css)
- Dark mode overrides go in the SAME CSS file using `[data-theme="dark"]` selector
- Animation gating: wrap all animations with `[data-a11y-motion="reduce"]` to disable them
- Event delegation: all handlers go in `initEventListeners()` via event delegation, never inline
- Mock data: use `MOCK_` prefix, all in `js/mock-data.js`
- New functions: use `Workflows.*` or `Chat.*` namespace in `app.js`
- CSS classes: kebab-case (e.g., `.wf-card-trigger-chip`)
- JS functions: Namespace.camelCase (e.g., `Workflows.renderLibrary()`)
- HTML IDs: camelCase (e.g., `#workflowPanel`)
- Beveled borders pattern: `border-color: light dark dark light` for 3D raised effect
- Font stack: ChicagoFLF (pixel headings), IBM Plex Mono (UI/code), DM Sans (body)
- Border radii: `var(--r-sm)` (3px), `var(--r-md)` (4px), `var(--r-lg)` (6px) — never raw px
- Dev server: `python3 -m http.server 8082` serves at http://localhost:8082/index.html
- Commit format: `workflows [task number]: [brief description]`
- Flow graph: `Workflows.renderFlowGraph(templateId, containerId, { compact, showStatus, runId })` renders SVG into any container. Arrowhead marker IDs namespaced by templateId. Node colors set via inline styles (CSS vars work in SVG inline styles). Status overrides use !important to beat inline colors.
- Flow graph CSS is in components.css under "FLOW GRAPH SVG" section header

---

## Iteration 1 — Task 1.1: Create MOCK_WORKFLOW_TEMPLATES
- What was done: All 7 templates created in previous iteration (commit 3225950), prd.json was not updated — fixed now.
- Files modified: js/mock-data.js (previous), scripts/ralph/prd.json (this iteration)
- Gotchas / learnings: None

## Iteration 2 — Task 1.2: Create MOCK_WORKFLOW_RUNS
- What was done: Added MOCK_WORKFLOW_RUNS object with two run entries keyed by threadId. wf-run-rentroll-047 (completed, all nodes completed, gate skipped, 1 inference exception at 94% confidence). wf-run-tener-12 (waiting at gate n6, 3 exceptions: conflicting-value, low-confidence at 67%, format-unknown). Node statuses reference actual node IDs from their respective templates.
- Files modified: js/mock-data.js
- Gotchas / learnings: MOCK_WORKFLOW_RUNS is keyed by threadId (not runId) for easy lookup when selecting threads. Node IDs must match the template's nodes array exactly.

## Iteration 3 — Task 1.3: Create MOCK_WORKFLOW_COMMANDS and update MOCK_THREADS
- What was done: Added MOCK_WORKFLOW_COMMANDS array with 5 commands (/rent-roll, /k1, /fees, /valuation, /dd) each with command, label, description, templateId, and optional argPlaceholder. Added 3 new workflow-linked threads to MOCK_THREADS: wf-run-rentroll-047 (completed, indicator: ready), tener-batch-12 (waiting, indicator: waiting), wf-create-dd (creation flow, no run). Added corresponding sidebar HTML in index.html with thread-indicator spans. Added .thread-indicator.waiting CSS with amber styling and dark mode override in layout.css. Added placeholder chat-thread divs for the 3 new threads.
- Files modified: js/mock-data.js, index.html, css/layout.css
- Gotchas / learnings: New threads use workflowRunId field (null for creation threads, threadId string for run threads). The "waiting" indicator reuses amber color like "needs-input". MOCK_WORKFLOW_RUNS is keyed by threadId, matching the workflowRunId field on threads. The /dd command has an argPlaceholder field for the entity argument.

## Iteration 4 — Task 1.4: Add new trigger icons to icons.js
- What was done: Added 6 new SVG icons to the ICONS object in js/icons.js: folderWatch (folder with eye dot), schedule (clock with hands), chatCommand (terminal window with >_ prompt), emailTrigger (envelope with V-flap), manualTrigger (play triangle), webhook (zigzag chain pattern). All follow existing conventions: viewBox="0 0 16 16", fill="none", stroke="currentColor", stroke-width="1.5", width/height 14.
- Files modified: js/icons.js
- Gotchas / learnings: Icon names map to triggerType values in templates: folder-watch → folderWatch, schedule → schedule, chat-command → chatCommand, email → emailTrigger, manual → manualTrigger. The webhook icon is extra for future use. Use icon('folderWatch') or data-icon="folderWatch" to render them.

## Iteration 5 — Task 2.1: Rebuild Library HTML and rendering function
- What was done: Replaced hardcoded workflow cards in index.html with empty containers (#wfStatsBody, #wfCardContainer). Created Workflows.renderLibrary() in app.js that reads MOCK_WORKFLOW_TEMPLATES, computes stats (total, active, total runs, failed), and builds card HTML dynamically. Each card has: header (title + status badge + trigger type chip + Run button visible on hover), body (description), footer (linked lesson chip with diamond icon + last run status dot + time). Added Workflows._triggerMeta lookup table mapping triggerType to icon name and label. Library renders on page load via IIFE. Card click delegation was already wired in initEventListeners.
- Files modified: index.html, js/app.js, css/workflows.css
- Gotchas / learnings: Workflows._triggerMeta maps triggerType strings to {icon, label} objects for rendering trigger chips. The old .wf-card-meta/.wf-meta-item classes are now unused CSS (cards use .wf-card-footer instead). Card click delegation was already in initEventListeners() — no changes needed there. The Run button uses data-action="run-card" for future event handling.

## Iteration 6 — Task 2.2: Rebuild workflow sidebar
- What was done: Replaced hardcoded sidebar HTML in index.html with empty containers (#wfActiveRuns, #wfSidebarList). Created Workflows.renderSidebar() that builds two sections: (1) Active Runs at top, filtered from MOCK_WORKFLOW_RUNS where status is 'running' or 'waiting' — shows template name, status indicator with colored dot, and thread title; (2) Template list below with trigger type icons from Workflows._triggerMeta, status badges, and last run info. Added event delegation for active run item clicks. Added full CSS for .wf-active-runs section (tinted violet background, border separator), .wf-active-run-item with hover/active/focus-visible states, .wf-run-running/.wf-run-waiting status colors, @keyframes wf-pulse gated by [data-a11y-motion="reduce"], .wf-side-trigger-icon, and dark mode overrides for all new classes.
- Files modified: index.html, js/app.js, css/layout.css
- Gotchas / learnings: MOCK_WORKFLOW_RUNS is keyed by threadId. Only the tener run (status: 'waiting') currently appears in Active Runs since rentroll is 'completed'. The wf-pulse animation class is reusable for any element needing a pulse effect. Active run items have data-thread-id and data-wf-id attributes for future navigation wiring (task 6.2).

## Iteration 8 — Task 3.1: Two-column detail layout
- What was done: Replaced the single-column wfDetail content with a two-column layout: left column (60%) contains .flow-graph-container with bevel border and placeholder text, right column (40%) contains description, meta row, tab bar (overview/schema/triggers/runs/lessons), and scrollable tab content. Updated Workflows.showWorkflowDetail() to populate meta row (status badge, trigger chip, version, creator) and overview tab (status/performance sections + recent activity) from MOCK_WORKFLOW_TEMPLATES data. Added Workflows._renderOverviewTab() helper. Added Workflows._currentTemplateId for state tracking. Scoped switchTab() to info column. Added full CSS with dark mode overrides.
- Files modified: index.html, js/app.js, css/workflows.css
- Gotchas / learnings: Tab names changed from overview/config/steps/runs to overview/schema/triggers/runs/lessons to match future tasks 4.1-4.4. The old hardcoded config/steps tabs are removed. Workflows._currentTemplateId stores which template is being viewed. switchTab() is now scoped to .wf-detail-info-col to avoid conflicts with other tab bars.

## Iteration 9 — Task 3.2: Flow graph SVG renderer
- What was done: Created Workflows.renderFlowGraph(templateId, containerId, options) in app.js. Reads nodes/edges from MOCK_WORKFLOW_TEMPLATES, calculates pixel positions from x/y grid coords, renders SVG with: rounded rect nodes (160x60 full, 100x36 compact), stroke/fill colors by node type per design spec, title text (mono 11px bold), description text (sans 10px, truncated at 35 chars, full mode only), lesson indicator dot, gate node dashed stroke, edges with arrowheads and branch labels at midpoints, run status color overrides (completed/running/waiting/pending/failed/skipped). Added Workflows._nodeColors lookup. Called from showWorkflowDetail() to render into #flowGraphContainer. Added comprehensive SVG CSS to components.css with full dark mode overrides, hover/active/focus-visible states, run status animations, and reduced motion gating.
- Files modified: js/app.js, css/components.css
- Gotchas / learnings: Arrowhead marker IDs are namespaced by templateId to avoid SVG ID collisions when multiple graphs are on the page. Edge routing uses straight lines for same-column nodes and a midpoint bend for cross-column connections. Node colors use inline style attributes since SVG fill/stroke can't be set via CSS classes alone (CSS custom properties work in inline styles). Compact mode reduces spacing (130x60 vs 200x100) and hides descriptions. Status classes use !important to override inline node type colors.

## Iteration 7 — Task 2.3: Library CSS
- What was done: All CSS from this task was already implemented during tasks 2.1 and 2.2. Verified completeness: .wf-card-trigger-chip, .wf-card-lesson-chip, .wf-card-run-status, .wf-card-run-btn (all in workflows.css with dark mode overrides and hover/active/focus-visible states), .wf-active-runs, .wf-active-run-item, @keyframes wf-pulse (all in layout.css with dark mode overrides and [data-a11y-motion="reduce"] gating). No code changes needed.
- Files modified: scripts/ralph/prd.json, scripts/ralph/progress.txt
- Gotchas / learnings: Tasks 2.1 and 2.2 proactively added all CSS alongside the HTML/JS, so 2.3 was already complete. Card-related CSS lives in workflows.css; sidebar-related CSS lives in layout.css (following the project's file organization convention).

## Iteration 10 — Task 3.3: Flow graph interaction (zoom, pan, node click)
- What was done: Added zoom (mouse wheel with cursor-centered scaling, range 0.3x–3x), pan (mouse drag on background), and node click selection to the flow graph. Wrapped SVG content in a `<g class="flow-graph-transform">` group (full mode only) that receives translate/scale transforms. Created Workflows._initGraphInteraction() called after SVG insertion. Created Workflows.selectNode(nodeId, templateId) that toggles a `.selected` class. Added event delegation for node clicks in initEventListeners(). Added CSS for selected node state (thicker stroke + violet outline), grab/grabbing cursors on container, and dark mode override for selection outline.
- Files modified: js/app.js, css/components.css
- Gotchas / learnings: Zoom/pan only applies to full mode (not compact). The transform group wraps edges and nodes but not defs. Pan is implemented via mousedown/mousemove/mouseup on document (mouseup/mousemove on document so drag works outside container bounds). Node clicks are excluded from pan initiation via `.closest('.flow-node')` check. State stored in Workflows._graphZoom, _graphPanX, _graphPanY. The _initGraphInteraction function is called each time renderFlowGraph runs, resetting state.

## Iteration 11 — Task 3.4: Node detail popover
- What was done: Created Workflows.openNodePopover(nodeId, templateId, anchorRect) that renders a .node-popover (320px wide, absolutely positioned over graph column). Shows: type label badge (color-coded by node type), title, full description, linked lesson detail (diamond icon + title + preview text if node has a lesson), and "Edit with Cosimo" button. Created Workflows.closeNodePopover() for cleanup. Updated the flow graph node click delegation in initEventListeners() to: (1) select node, (2) open popover (full mode only, not compact), (3) dismiss on click-outside, (4) dismiss on close button, (5) wire "Edit with Cosimo" to UI.openCosimoPanel(). Added comprehensive CSS to components.css with full dark mode overrides, hover/active/focus-visible states on all interactive elements, and reduced motion gating. Added position:relative to .wf-detail-graph-col and removed overflow:hidden so popover isn't clipped.
- Files modified: js/app.js, css/components.css, css/workflows.css
- Gotchas / learnings: Dark mode tokens don't use dk- prefix — the same token names (--taupe-1 through --taupe-5, --white, --off-white) are redefined under [data-theme="dark"] in tokens.css. Popover is inserted into .wf-detail-graph-col (which has position:relative) and positioned using getBoundingClientRect() relative to the column. The _activePopover reference tracks the current popover for click-outside dismissal. Only full-mode graphs get popovers (compact SVGs with .flow-graph-compact class are skipped).

## Iteration 12 — Task 4.1: Schema tab
- What was done: Created Workflows._renderSchemaTab(data) that renders Input Schema section (description + field rows with name, type badge, required indicator, description, enum options) and Output Schema section (format file-chip, destination path, column chips). Called from showWorkflowDetail(). Added comprehensive CSS to components.css: .schema-field-row, .schema-field-header, .schema-field-name, .schema-type-badge with per-type color variants (string=blue, number=violet, date=amber, currency=green, enum=berry, boolean=taupe), .schema-required (red), .schema-field-desc, .schema-option-chip, .schema-output-meta, .schema-destination, .schema-columns-label, .schema-column-chip. Full dark mode overrides with 0.12 alpha fills.
- Files modified: js/app.js, css/components.css
- Gotchas / learnings: Schema type badges reuse the file-chip color pattern (type-specific color + border + light bg). The output format chip reuses existing .file-chip classes (already have xlsx, pdf, csv, etc. styled). Enum fields have an options array that gets rendered as small chips below the description.

## Iteration 13 — Task 4.2: Triggers tab
- What was done: Created Workflows._renderTriggersTab(data) that renders trigger items from template triggerConfig. Each trigger item shows: icon in a tinted circle, type label (mono bold), "Primary" badge for the main trigger type, and config detail (folder path, cron schedule, chat command code chip, or manual text). Handles templates where primary type is chat-command (shows command detail on primary item) and templates with secondary chat commands (e.g., rent-roll has folder-watch primary + /rent-roll chat command). Empty state for templates with no triggers. "Add Trigger" dashed button using existing .add-source-btn class. Called from showWorkflowDetail(). Added comprehensive CSS to components.css with full dark mode overrides.
- Files modified: js/app.js, css/components.css
- Gotchas / learnings: triggerConfig can have multiple trigger fields (e.g., watchPath + chatCommand). The primary trigger type comes from data.triggerType, but chat commands may exist as a secondary trigger. The trigger-detail-command uses a <code> element styled as a violet-tinted chip for command strings.

## Iteration 14 — Task 4.3: Runs tab
- What was done: Created Workflows._renderRunsTab(data) that renders run history from template.recentRuns. Summary bar shows total runs, success rate, and avg duration. Each run row uses existing .run-row pattern with status dot, run ID, trigger detail, timestamp, and duration. Rows with threadId get .run-row-linked class (pointer cursor, violet hover/active/focus-visible states, data-thread-id attribute for Phase 6 navigation wiring). Rows without threadId get .cursor-default. Empty state for templates with no runs. Added CSS to components.css with full dark mode overrides. Called from showWorkflowDetail().
- Files modified: js/app.js, css/components.css
- Gotchas / learnings: The existing .run-row base class has cursor:pointer by default. Overview tab's run rows use .cursor-default utility to override. Runs tab uses .run-row-linked for threadId rows (keeps pointer) and .cursor-default for non-linked rows. The data-action="run-row-click" attribute on linked rows is ready for Phase 6 event delegation wiring.

## Iteration 15 — Task 4.4: Lessons tab
- What was done: Created Workflows._renderLessonsTab(data) that cross-references template.linkedLessons with MOCK_LESSONS. Each lesson card shows: diamond icon, title, scope badge (company=blue, user=violet), "Used by" node chips (found by filtering template nodes where node.lesson matches the lessonId), 2-line preview text, and author/updated meta. Empty state for templates with no linked lessons. "Link Lesson" dashed button using existing .add-source-btn pattern. Called from showWorkflowDetail(). Added comprehensive CSS to components.css with full dark mode overrides.
- Files modified: js/app.js, css/components.css
- Gotchas / learnings: MOCK_LESSONS is keyed by lesson ID strings (e.g., 'rent-roll-format'). Node.lesson field references these same IDs. Templates like covenant, tener-valuation, and due-diligence have empty linkedLessons arrays, so they show the empty state. The due-diligence template has no linkedLessons but its node n4 (Process Rent Rolls) references 'rent-roll-format' — this is intentional since the lesson isn't "linked" at the template level.

## Iteration 17 — Task 5.2: Compact flow graph with run status
- What was done: The compact flow graph rendering and run status CSS were already implemented in tasks 3.2 (renderFlowGraph with compact/showStatus/runId options) and 5.1 (wiring into workflow panel). This iteration fixed the compact SVG sizing to scale responsively within the ~380px panel width. Changed compact SVG to use `width="100%"` with `preserveAspectRatio="xMidYMid meet"` so wider graphs (like tener-valuation with 3 columns) scale down to fit. Added `overflow: hidden` and `.flow-graph-compact { max-width: 100%; height: auto }` to the panel graph container CSS.
- Files modified: js/app.js, css/chat.css
- Gotchas / learnings: Compact SVGs for templates with wide branching (x range > 2 columns) would overflow a 380px panel at 130px x-spacing. Using viewBox + width="100%" + preserveAspectRatio makes SVG scale to fit any container width. The rent-roll graph (2 columns, ~290px) fits naturally; tener-valuation (3 columns, ~420px) now scales down proportionally.

## Iteration 18 — Task 5.3: Workflow chat thread content (completed run)
- What was done: Replaced placeholder content in thread-wf-run-rentroll-047 with full 5-message conversation following CLAUDE.md Thread A script. Message 1: user with /rent-roll command chip (.wf-command-chip) and 3 PDF file attachment chips (.wf-file-chip). Message 2: Cosimo run-started with artifact showing run summary (.wf-run-summary). Message 3: Cosimo extraction results (142 units, floor inference at 94% confidence). Message 4: Cosimo output ready with spreadsheet preview artifact using .data-tbl (5 sample rows with unit/floor/tenant/lease/rent/sqft/status columns, status chips for Occupied/Vacant/Notice). Message 5: User confirmation. Added comprehensive CSS to chat.css: .wf-command-chip (mono, violet bg, white text, pill), .wf-file-attachments/.wf-file-chip (taupe bordered chips), .wf-run-summary (label/value rows in artifact), .wf-status-chip with .occupied/.vacant/.notice variants (green/red/amber), full dark mode overrides with increased alpha fills.
- Files modified: index.html, css/chat.css
- Gotchas / learnings: Chat.selectThread() already handles thread switching via active class toggle — no JS changes needed. The thread uses chat-scroll wrapper (same as other threads) for scrolling. File attachment chips use emoji 📄 for PDF icon since SVG icons would be overkill here. The .wf-command-chip, .wf-file-chip, .wf-run-summary, and .wf-status-chip classes are reusable for tasks 5.4 and 5.5.

## Iteration 19 — Task 5.4: Workflow chat thread content (gate run)
- What was done: Replaced placeholder content in thread-tener-batch-12 with full 7-message conversation following CLAUDE.md Thread B script. Message 1: user with /valuation command chip and folder attachment (47 files). Message 2: Cosimo run-started with artifact showing run metadata. Message 3: Cosimo gate message (.msg-gate) with exception table artifact (.data-tbl) showing 3 problem files (conflicting values, poor OCR, unexpected TIFF format). Message 4: Cosimo gate follow-up asking about conflicting assessed values, with ⏸ Awaiting Review chip (.msg-gate-chip). Message 5: User response choosing page 7 amended value. Message 6: Cosimo acknowledgment resuming processing. Message 7: Cosimo completion with all 47 files processed. Added CSS for .msg-gate (3px amber left border, rgba amber background tint, rounded right corners) and .msg-gate-chip (amber mono chip matching model-badge size/style). Full dark mode overrides with increased alpha.
- Files modified: index.html, css/chat.css
- Gotchas / learnings: .msg-gate class goes on the .ai-block element (not a wrapper), creating the amber left border + tint directly on the message. The .msg-gate-chip uses same sizing pattern as .model-badge (10px mono, pill shape). Gate messages 3 and 4 both have the .msg-gate class and .msg-gate-chip — message 3 has the exception table, message 4 has the specific question. Messages 6 and 7 are normal (non-gate) Cosimo messages after the user responds.

## Iteration 16 — Task 5.1: Workflow Context Panel structure
- What was done: Added #workflowPanel to index.html alongside #filePanel (same side, same structural pattern). Panel has: header (template name, run ID, status badge with color coding, close button), graph container (#wfPanelGraph for compact flow graph — renders via Workflows.renderFlowGraph with compact+showStatus options), and scrollable details section with Input manifest (file items with icons/names/sizes), Exceptions (colored dots by type, type label, description, confidence), and Output manifest. Created Chat.openWorkflowPanel(runId) that populates all sections from MOCK_WORKFLOW_RUNS + MOCK_WORKFLOW_TEMPLATES data. Created Chat.closeWorkflowPanel(). Updated Chat.selectThread() to auto-open panel when thread has workflowRunId, auto-close when it doesn't. Added resize handle (#wfPanelResizeHandle) wired into the existing initResize() IIFE. Close button wired in initEventListeners(). Added comprehensive CSS to chat.css with full dark mode overrides.
- Files modified: index.html, js/app.js, css/chat.css
- Gotchas / learnings: Workflow panel uses same resize pattern as file panel (initResize with 'left' side). Panel auto-opens when thread has workflowRunId field pointing to a valid MOCK_WORKFLOW_RUNS key. The openWorkflowPanel() also closes the file panel since they share the same side. Exception dot colors vary by type: inference=blue, low-confidence=amber, conflicting-value=red, format-unknown=taupe. The compact flow graph call is already in place for task 5.2 to extend.

## Iteration 21 — Task 5.6: Chat command autocomplete
- What was done: Created command autocomplete system triggered when user types `/` as first character in any chat contenteditable input. Dynamically creates and positions a `.wf-command-autocomplete` dropdown above the `.input-area`. Populated from MOCK_WORKFLOW_COMMANDS with filtering by typed text. Each item shows: command (mono bold violet), label (sans, muted), description (small sans). Full keyboard navigation: ArrowUp/Down to navigate, Enter to select, Escape to dismiss. Mouse click to select. Selecting inserts command text; if command has argPlaceholder (e.g., /dd [company name]), it's appended and auto-selected for easy replacement. Dismiss on click-outside. Created Chat.showCommandAutocomplete(), Chat.hideCommandAutocomplete(), Chat.selectCommand(), Chat._acNavigate(), Chat._acConfirm() functions. Added event delegation in initEventListeners() for input, keydown, and click events. Added comprehensive CSS to chat.css with full dark mode overrides, hover/active/focus-visible states. Added position:relative to .input-area for dropdown positioning.
- Files modified: js/app.js, css/chat.css
- Gotchas / learnings: The dropdown is a single shared DOM element (Chat._acDropdown) moved between input areas as needed, avoiding duplicating HTML in every thread. Uses document-level event delegation for input/keydown/click rather than per-element listeners, since contenteditable inputs are static HTML. The dropdown is inserted as first child of .input-area (which has position:relative) and positioned with bottom:100% to appear above the input row. The _acInputEl reference tracks which input triggered the autocomplete for command insertion.

## Iteration 22 — Task 5.7: Thread sidebar workflow indicators
- What was done: Added ⚙ gear icon (.thread-wf-icon) before title text on 3 workflow thread items (wf-run-rentroll-047, tener-batch-12, wf-create-dd) in index.html sidebar. Added .thread-wf-icon CSS (10px, taupe-3 color, inline display). Added .thread-indicator.running CSS (violet-3 color/border/fill, wf-pulse animation). Gated running animation with [data-a11y-motion="reduce"]. Added full dark mode overrides for both .thread-wf-icon and .thread-indicator.running.
- Files modified: index.html, css/layout.css
- Gotchas / learnings: The waiting indicator was already implemented in task 1.3. The running indicator reuses the existing @keyframes wf-pulse from task 2.2. Gear icon uses the ⚙ character (U+2699) rather than an SVG icon for simplicity at 10px size. All 3 workflow threads get the gear icon regardless of whether they have a workflowRunId (wf-create-dd is a creation thread with null workflowRunId but is still workflow-related).

## Iteration 23 — Task 6.1: Wire Library to Detail to Chat navigation
- What was done: Wired all navigation paths between Workflows and Chat views. (1) Library card Run button (data-action="run-card") navigates to chat run thread via Workflows.navigateToRun(). (2) Detail Run button and actions dropdown "Run Now" both call navigateToRun(). (3) Runs tab row clicks with threadId navigate to that chat thread via Workflows.navigateToThread(). (4) Back button already worked. (5) Added Workflows._lastViewState to preserve detail/listing state when switching tabs — switchMode('workflows') now restores last-viewed template detail instead of always resetting to listing. Created helper functions: Workflows._findRunThread(templateId), Workflows.navigateToRun(templateId), Workflows.navigateToThread(threadId).
- Files modified: js/app.js
- Gotchas / learnings: Workflows._lastViewState stores {templateId} when viewing a detail, set to null on showWorkflowListing(). The navigateToRun() helper searches MOCK_WORKFLOW_RUNS by templateId to find the threadId, then delegates to navigateToThread(). The Run button on library cards uses e.stopPropagation() to prevent the card click from also firing showWorkflowDetail.

## Iteration 20 — Task 5.5: Workflow chat thread content (creation flow)
- What was done: Replaced placeholder content in thread-wf-create-dd with full 9-message conversation following CLAUDE.md Thread C script. Message 1: user wants to create DD workflow. Message 2: Cosimo asks clarifying questions. Message 3: user describes requirements (RE acquisitions, 5 doc types). Message 4: Cosimo shows proposed flow graph as artifact (renders via Workflows.renderFlowGraph into #ddFlowGraphInitial). Message 5: user asks for cross-reference step. Message 6: Cosimo shows updated flow graph artifact (#ddFlowGraphUpdated). Message 7: Cosimo suggests /due-diligence chat command trigger with .wf-command-inline styling. Message 8: user names it /dd. Message 9: Cosimo confirms with /dd command chip. Created Chat._renderDDFlowGraphs() helper called via requestAnimationFrame from selectThread when wf-create-dd is selected. Added noInteraction option to renderFlowGraph to skip zoom/pan for artifact-embedded graphs. Added CSS for .wf-artifact-graph (scrollable container, min-height) and .wf-command-inline (violet-tinted inline code chip). Full dark mode overrides.
- Files modified: index.html, js/app.js, css/chat.css
- Gotchas / learnings: Flow graphs in chat artifacts use { compact: false, noInteraction: true } to render full-size but without zoom/pan (avoids shared state conflicts between multiple graphs on the same page). The renderFlowGraph noInteraction option skips _initGraphInteraction(). Chat._renderDDFlowGraphs() checks for existing SVG before re-rendering to avoid duplicates on repeated thread selection. Both artifact graphs use the same due-diligence template data (the "initial" and "updated" distinction is conceptual — the mock data already includes the cross-reference node).

